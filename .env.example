# Source of truth for backend environment variables:
#   server/.env.example
#
# This root file is intentionally a pointer to avoid duplicate/conflicting keys.
# Copy server/.env.example to server/.env and configure values there.
